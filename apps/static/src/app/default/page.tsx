import React from "react"

// import { getStripeProducts } from "@/features/stripe/server.actions"

import PageClient from "./page.client"

export default async function Page() {
  // const data = await getStripeProducts()
  return <PageClient />
}
